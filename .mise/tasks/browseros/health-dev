#!/usr/bin/env bash
set -euo pipefail
# health-dev — Check BrowserOS dev instance health (port 9011)
curl -sf "http://localhost:9011/health" | python3 -m json.tool 2>/dev/null || echo "Dev instance not running"
